import SwiftUI
import GhosttyKit

struct SidePanelView: View {
    @ObservedObject var viewModel: SidePanelViewModel

    var body: some View {
        Color.clear
            .frame(width: 280)
    }
}
