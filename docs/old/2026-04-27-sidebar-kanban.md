# ⚠️ OBSOLETE

Superseded by `ghostty-kanban-full.md`.

This document described the old `Project`/`Card`/`CardStatus` implementation. The current implementation uses `KanbanTask`/`Session`/`Status` from the kanbangui project.

See `PLAN.md` for implementation summary.
